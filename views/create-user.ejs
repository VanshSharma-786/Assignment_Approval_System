<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create User | University Assignment Approval</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="admin-layout">
    <header class="admin-header-bar">
      <a href="/admin/dashboard" class="admin-logo-title" style="color: inherit;">Admin Dashboard</a>
      <div class="navbar-menu" style="margin: 0;">
        <div class="user-profile">
          <div class="user-avatar" id="avatar-display">
            <%= user.email.charAt(0).toUpperCase() %>
          </div>
          <span><%= user.email %></span>
        </div>
        <form action="/auth/logout" method="GET" style="margin: 0;">
          <button type="submit" class="btn-logout" id="logout-btn">Sign Out</button>
        </form>
      </div>
    </header>

    <div class="admin-container">
      <aside class="admin-sidebar">
        <h2 class="sidebar-title">Navigation</h2>
        <nav class="sidebar-menu">
          <a href="/admin/departments" class="sidebar-link">Departments</a>
          <a href="/admin/users" class="sidebar-link">Users</a>
          <a href="/admin/users/add" class="sidebar-link" style="background-color: var(--primary); color: var(--text-inverse);">Add User</a>
        </nav>
      </aside>

      <main class="admin-content">
        <header class="dashboard-header" style="margin-bottom: 24px;">
          <h2 class="dashboard-title" style="font-size: 22px;">Create User Account</h2>
        </header>

        <% if (error) { %>
          <div class="alert alert-danger" role="alert" style="max-width: 600px;">
            <span class="alert-icon">!</span>
            <span><%= error %></span>
          </div>
        <% } %>

        <div class="card-form">
          <form action="/admin/users/create" method="POST">
            <div class="form-group">
              <label for="name" class="form-label">Full Name</label>
              <input 
                type="text" 
                id="name" 
                name="name" 
                class="form-input" 
                placeholder="e.g. John Doe" 
                required
              >
            </div>

            <div class="form-group">
              <label for="email" class="form-label">Email Address</label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                class="form-input" 
                placeholder="e.g. john.doe@university.edu" 
                required
              >
            </div>

            <div class="form-group">
              <label for="password" class="form-label">Password</label>
              <input 
                type="password" 
                id="password" 
                name="password" 
                class="form-input" 
                placeholder="Enter account password" 
                required
              >
            </div>

            <div class="form-group">
              <label for="phone" class="form-label">Phone Number</label>
              <input 
                type="tel" 
                id="phone" 
                name="phone" 
                class="form-input" 
                placeholder="e.g. +1 (555) 019-2041"
              >
            </div>

            <div class="form-group">
              <label for="departmentId" class="form-label">Department</label>
              <div class="input-wrapper">
                <select id="departmentId" name="departmentId" class="form-input form-select" required>
                  <option value="">Select Department</option>
                  <% departments.forEach(dept => { %>
                    <option value="<%= dept.id %>"><%= dept.name %></option>
                  <% }); %>
                </select>
              </div>
            </div>

            <div class="form-group">
              <label for="role" class="form-label">Role</label>
              <div class="input-wrapper">
                <select id="role" name="role" class="form-input form-select" required>
                  <option value="">Select Role</option>
                  <option value="student">Student</option>
                  <option value="professor">Professor</option>
                  <option value="hod">HOD</option>
                </select>
              </div>
            </div>

            <div style="display: flex; gap: 16px; margin-top: 32px;">
              <button type="submit" class="btn btn-primary" style="flex: 1;">Create Account</button>
              <a href="/admin/users" class="btn btn-secondary" style="flex: 1; border: 1px solid var(--border-color); color: var(--text-main); text-align: center;">Cancel</a>
            </div>
          </form>
        </div>
      </main>
    </div>

    <footer class="admin-footer">
      <p>&copy; 2025 Admin Dashboard. All rights reserved.</p>
    </footer>
  </div>
</body>
</html>
